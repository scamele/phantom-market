{
  "name": "phantom-relay-league",
  "version": "1.0.0",
  "description": "Phantom Relay League - FHE-powered multi-leg relay prediction league",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.cjs --network sepolia",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "dotenv": "^16.4.0"
  },
  "dependencies": {
    "@fhevm/solidity": "^0.9.1",
    "ethers": "^6.13.4",
    "dotenv": "^16.4.5"
  }
}
